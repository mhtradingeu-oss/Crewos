export { PageHeader } from "@/components/shell/page-header";

// Backward-compatible re-export for legacy imports. Prefer using PageHeader from shell directly.
export { HeaderAction } from "@/components/ui/header-action";

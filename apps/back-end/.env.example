# Local backend environment example (copy to `apps/back-end/.env` and adjust before starting)
# These values are for local development when running the backend outside of Docker.

NODE_ENV=development
PORT=4000
REDIS_URL="redis://localhost:6379"
JWT_SECRET="replace-this-secret-locally"
DATABASE_URL="postgresql://mhos_user:mhos_password@localhost:5432/mhos_dev?schema=public"

# These credentials match the defaults used by the Prisma dev seeds and can be overridden per developer.
ADMIN_EMAIL=dev.superadmin@example.com
ADMIN_PASSWORD=Dev12345!

# Update the credentials above if you run another PostgreSQL instance.

// SecurityGovernanceRepository: Move all prisma queries from security-governance.service.ts here
import { prisma } from '../../prisma.js';

export const SecurityGovernanceRepository = {
  // Add methods as needed for security governance
};

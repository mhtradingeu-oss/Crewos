// VirtualOfficeRepository: Move all prisma queries from virtual-office.service.ts here
import { prisma } from '../../prisma.js';

export const VirtualOfficeRepository = {
  // Add methods as needed for virtual office
};

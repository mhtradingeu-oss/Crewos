// StandPosRepository: Move all prisma queries from stand-pos.service.ts here
import { prisma } from '../../prisma.js';

export const StandPosRepository = {
  // Add methods as needed for stand-pos
};

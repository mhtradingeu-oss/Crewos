import { forgotPasswordSchema, loginSchema, registerSchema } from "@mh-os/shared";

export { loginSchema, registerSchema, forgotPasswordSchema };
export type { LoginDto, RegisterDto, ForgotPasswordDto } from "@mh-os/shared";

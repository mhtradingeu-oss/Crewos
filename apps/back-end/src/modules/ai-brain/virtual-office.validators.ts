import { z } from "zod";

// Phase-B stubs (NO validation)
export const virtualOfficeMeetingSchema = z.any();
export type VirtualOfficeMeetingDto = any;
// All domain types and schemas have been moved to @mh-os/shared. Only UI/View/Props types may remain here.

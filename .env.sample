# ── Core Backend Environment ─────────────────────
# PostgreSQL configuration (used by Docker + local dev)
#POSTGRES_USER=mhos_user
#POSTGRES_PASSWORD=mhos_password
#POSTGRES_DB=mhos_dev
POSTGRES_PORT=5432

DATABASE_URL=postgresql://gharabli:gharabli123$@localhost:5432/mhos_dev?schema=public
# Backend-specific secrets live in apps/back-end/.env for local runs and Docker Compose
# leverages the same file (see docker-compose.yml -> backend.env_file).

# Base URLs for services
BACKEND_PORT=4000
FRONTEND_PORT=3000

API_BASE_URL=http://localhost:41000/api/v1
NEXT_PUBLIC_API_URL=http://localhost:41000/api/v1

# ── AI / LLM Providers ───────────────────────────
# Main OpenAI key (used by backend AI service & Codex if needed)
OPENAI_API_KEY=your-openai-key-here
AI_INTEGRATIONS_OPENAI_API_KEY=${OPENAI_API_KEY}

# Optional: custom base URL if using a proxy or Azure
OPENAI_BASE_URL=https://api.openai.com/v1

# Provider switch (openai | ollama | mock)
AI_PROVIDER=openai

# ── Misc / Telemetry / Debug ─────────────────────
LOG_LEVEL=debug

# Backend env for Docker-based development lives under apps/back-end/.env
# Copy apps/back-end/.env.example to apps/back-end/.env and adjust credentials as needed.
